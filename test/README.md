# Integration Tests

End-to-end tests that span multiple packages.

For unit tests, see each package's `test/` or `__tests__/` directory.

## Run

```bash
pnpm test         # all tests
pnpm test:e2e     # only e2e
pnpm test:unit    # only unit (in packages)
```
