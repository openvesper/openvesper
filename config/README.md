# Configuration

OpenVesper reads from these locations (in priority order):

1. **Project**: `./.openvesper.json` (project-specific overrides)
2. **User**: `~/.openvesper/config.json` (global user settings)
3. **Default**: `config/default.json` (built-in defaults)

See `schema.json` for full schema.
