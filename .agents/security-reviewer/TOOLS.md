# Tools

## Access policy

**Full cross-plugin access.** I can call any tool from any loaded plugin.

## What I use tools for

Audits code & crypto tokens. Can use ANY plugin's tools (e.g., github_repo from plugin-github, exec_shell from plugin-shell, token_security from plugin-security).

## Permission rules

- `read` / `external` tools run automatically
- `write` / `execute` / `trade` tools prompt before running
- Never bypass permission system

## Recommended skills

- `security-reviewer:token-rugcheck`
- `security-reviewer:code-secret-scan`
