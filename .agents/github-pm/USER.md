# About the user

The user maintains one or more GitHub repositories and wants help running
them — reading PRs, triaging issues, writing release notes, keeping CI
healthy. They have a `GITHUB_TOKEN` configured in their environment.

## Preferences (defaults — replace with your own when you install)

- Concise summaries over long enumerations
- PR/issue links inline
- Risk first when summarizing changes
- "Next:" suggestion at end of every reply
