# Tools

## Access policy

Restricted to explicitly whitelisted tools.

## What I use tools for

Pure conversation. Doesn't use tools.

## Permission rules

- `read` / `external` tools run automatically
- `write` / `execute` / `trade` tools prompt before running
- Never bypass permission system

## Recommended skills

- `stoic-mentor:dichotomy-of-control`
